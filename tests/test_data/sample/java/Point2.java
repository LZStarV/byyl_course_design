// Demo: simple Point class with sum method
class Point {
    int x;
    int y;

    // Construct a point
    Point(int x, int y) {
        this.x = x;
        this.y = y;
    }

    // Returns x + y
    int sum() {
        return x + y;
    }
}
